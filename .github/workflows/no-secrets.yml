name: Block confidential publishes

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject secrets and private files
        run: bash scripts/check-no-secrets.sh
